server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: infra-boot
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/vibe_admin?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Qwer098123
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 5s
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

security:
  jwt:
    secret: vibe-admin-jwt-secret-key-must-be-longer-than-256-bits-change-me
    expiration: 7200

    header: Authorization
    token-prefix: "Bearer "
